
{
  "entities": {
    "Quotation": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Quotation",
      "type": "object",
      "description": "Represents a quotation generated by the Quotation Builder.",
      "properties": {
        "quotationId": {
          "type": "string",
          "description": "Unique identifier for the Quotation entity."
        },
        "clientDetails": {
          "type": "string",
          "description": "JSON string containing the client's details."
        },
        "products": {
          "type": "string",
          "description": "JSON string representing the products included in the quotation."
        },
        "pricing": {
          "type": "string",
          "description": "JSON string representing the pricing details for the quotation."
        },
        "totals": {
          "type": "string",
          "description": "JSON string representing the total costs for the quotation."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the quotation was created, represented as an ISO 8601 date-time string.",
          "format": "date-time"
        },
        "createdBy": {
          "type": "string",
          "description": "Identifier of the user who created the quotation."
        },
        "status": {
          "type": "string",
          "description": "The status of the quotation (e.g., ACTIVE, ARCHIVED)."
        }
      },
      "required": [
        "quotationId",
        "clientDetails",
        "products",
        "pricing",
        "totals",
        "createdAt",
        "createdBy",
        "status"
      ]
    },
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile and system role.",
      "properties": {
        "email": {
          "type": "string",
          "description": "The user's corporate email address."
        },
        "role": {
          "type": "string",
          "enum": [
            "admin",
            "user"
          ],
          "description": "The assigned access level for the user."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the user profile was created."
        }
      },
      "required": [
        "email",
        "role",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/quotations/{quotationId}",
        "definition": {
          "entityName": "Quotation",
          "schema": {
            "$ref": "#/backend/entities/Quotation"
          },
          "description": "Stores quotation data. Includes denormalized 'createdBy' field for authorization independence.",
          "params": [
            {
              "name": "quotationId",
              "description": "Unique identifier for the quotation."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores user profile data and roles. Restricted to owner-access.",
          "params": [
            {
              "name": "userId",
              "description": "The Firebase Auth UID of the user."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure supports owner-based access control for both user profiles and quotations. The 'users' collection stores roles (like 'admin') required for accessing protected routes and administrative features. Quotations are stored at the top level to facilitate efficient AI-driven queries for active deals based on creator UID."
  }
}
