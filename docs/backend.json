
{
  "entities": {
    "SiteVisitExpense": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SiteVisitExpense",
      "type": "object",
      "description": "Represents an employee's expense record for a site visit.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the SiteVisitExpense entity."
        },
        "employeeName": {
          "type": "string",
          "description": "The full name of the employee who incurred the expense for the site visit."
        },
        "companyName": {
          "type": "string",
          "description": "The name of the company associated with the site visit expense."
        },
        "fromLocation": {
          "type": "string",
          "description": "The starting location of the site visit route."
        },
        "toLocation": {
          "type": "string",
          "description": "The destination location of the site visit route."
        },
        "goingAmount": {
          "type": "number",
          "description": "The monetary amount spent on travel for the outgoing leg of the site visit."
        },
        "returnAmount": {
          "type": "number",
          "description": "The monetary amount spent on travel for the return leg of the site visit."
        },
        "totalAmount": {
          "type": "number",
          "description": "The auto-calculated total travel expense, which is the sum of 'Going Amount' and 'Return Amount'."
        },
        "quotationStatus": {
          "type": "string",
          "description": "The current status of the quotation associated with the expense. Possible values are 'Sent', 'Pending', or 'Approved'."
        },
        "quotationReferenceNumber": {
          "type": "string",
          "description": "A unique reference number provided for the associated quotation."
        },
        "remarks": {
          "type": "string",
          "description": "Any additional notes or comments pertinent to the site visit expense."
        },
        "visitDate": {
          "type": "string",
          "description": "The specific date when the site visit took place.",
          "format": "date"
        },
        "expenseApprovalStatus": {
          "type": "string",
          "description": "The overall approval status of the expense record itself. Possible values are 'Pending', 'Approved', or 'Rejected'."
        }
      },
      "required": [
        "id",
        "employeeName",
        "companyName",
        "fromLocation",
        "toLocation",
        "goingAmount",
        "returnAmount",
        "totalAmount",
        "quotationStatus",
        "visitDate",
        "expenseApprovalStatus"
      ]
    },
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile and assigned role within the system.",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address associated with the user profile."
        },
        "role": {
          "type": "string",
          "enum": ["admin", "user"],
          "description": "The access level assigned to the user."
        }
      },
      "required": ["email", "role"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores individual user profiles. Each user can read and write their own profile to manage their personal data and view their assigned role.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier (UID) of the user."
            }
          ]
        }
      },
      {
        "path": "/roles_admin/{adminId}",
        "definition": {
          "entityName": "admin",
          "schema": {
            "type": "object",
            "properties": {}
          },
          "description": "A collection used for Database Access Control (DBAC). The existence of a document at `/roles_admin/{uid}` signifies that the user with that 'uid' has administrative privileges. Used to authorize access to admin-only resources.",
          "params": [
            {
              "name": "adminId",
              "description": "The unique identifier (UID) of the administrative user."
            }
          ]
        }
      },
      {
        "path": "/site_visit_expenses/{expenseId}",
        "definition": {
          "entityName": "SiteVisitExpense",
          "schema": {
            "$ref": "#/backend/entities/SiteVisitExpense"
          },
          "description": "Stores all site visit expense records submitted and managed by administrators.",
          "params": [
            {
              "name": "expenseId",
              "description": "The unique identifier for a specific site visit expense record."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure utilizes structural segregation and DBAC for secure, enterprise-grade access control. User profiles are stored at '/users/{userId}' for personal data management, while sensitive admin operations are gated by existence checks in the '/roles_admin' collection."
  }
}
